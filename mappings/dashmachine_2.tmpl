[Settings]
theme = dark
accent = blue
background = None
roles = admin,user,public_user
home_access_groups = admin_only
settings_access_groups = admin_only
custom_app_title = DashMachine
sidebar_default = open

[sottey]
role = admin
password = 
confirm_password = 

{{- range .Site.Pages }}
  {{- range .Sections }}
[{{ .Title }}]
type = collection
icon = collections_bookmark
urls = {{- range $i, $item := .Items }}{{ if $i }},{{ end }}{"url": "{{ $item.URL }}", "icon": "{{ $item.Icon }}", "name": "{{ $item.Title }}", "open_in": "{{ if eq $item.Target "_blank" }}new_tab{{ else }}this_tab{{ end }}"}{{- end }}

  {{- end }}
{{- end }}